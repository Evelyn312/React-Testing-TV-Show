import React from "react";
import { render} from "@testing-library/react";
import App from "./App"

test("page render with api", () => {

    //arrange(render component and set up mock data)
    render(<App />);


    //act


    //assert
});